debug: false
# 日志
logging:
  pattern:
    level: debug
# 数据源
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:p6spy:mysql://192.168.10.166:12306/jxc?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ENC(m7znO0dJTuxxjJT1KQGOGQ==)
    password: ENC(m7znO0dJTuxxjJT1KQGOGQ==)
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  hikari:
    minimum-idle: 3
    maximum-pool-size: 10
    max-lifetime: 3000
    connection-test-query: select 1
  devtools:
    livereload:
      enabled: true
    restart:
      additional-paths: src/main/java
# 校验表结构
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQL5Dialect
cache:
  type: map
  cache-names: sys
jwt:
  cookie:
    maxAge: 3600